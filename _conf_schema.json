{
  "limits": {
    "description": "基础限制设置",
    "type": "object",
    "items": {
      "default_daily_limit": {
        "description": "默认日限次数",
        "type": "int",
        "default": 20,
        "hint": "每个用户每天可调用 LLM 的次数（0=不限制）。未设置特定用户/群组限额时生效，可被用户/群组特定限制覆盖。"
      },
      "default_weekly_limit": {
        "description": "默认周限次数",
        "type": "int",
        "default": 0,
        "hint": "每个用户每周可调用 LLM 的次数（0=不启用）。周重置发生在「每周重置日」+「每日重置时间」时刻。"
      },
      "default_monthly_limit": {
        "description": "默认月限次数",
        "type": "int",
        "default": 0,
        "hint": "每个用户每月可调用 LLM 的次数（0=不启用）。月重置发生在「每月重置日」+「每日重置时间」时刻，超出当月天数自动取最后一天。"
      },
      "daily_reset_time": {
        "description": "每日重置时间",
        "type": "string",
        "default": "00:00",
        "hint": "日/周/月限共用此时间点重置（HH:MM 格式，0≤H≤23，0≤M≤59）。如 00:00 表示凌晨零点，06:30 表示早上6点半。"
      },
      "weekly_reset_day": {
        "description": "每周重置日",
        "type": "int",
        "default": 1,
        "hint": "周限在此日 + 每日重置时间 时刻清零。1=周一，7=周日。例如设为 1+00:00，即每周一凌晨清零周计数。"
      },
      "monthly_reset_day": {
        "description": "每月重置日",
        "type": "int",
        "default": 1,
        "hint": "月限在此日 + 每日重置时间 时刻清零（1-28）。超出当月天数自动取最后一天，如设为 31 会调整为月末。"
      },
      "enabled_limit_types": {
        "description": "启用的限制维度",
        "type": "list",
        "default": ["daily"],
        "items": {
          "type": "string"
        },
        "options": ["daily", "weekly", "monthly", "timeperiod"],
        "hint": "选择同时生效的限流维度，任一维度超额即拦截。daily=日限，weekly=周限，monthly=月限，timeperiod=自定义时间段。"
      },
      "exempt_users": {
        "description": "豁免用户ID列表",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "名单内用户完全跳过所有限流检查（日/周/月/时间段/群组限制均不生效）。每个元素为一个用户ID，建议仅管理员使用。"
      },
      "priority_users": {
        "description": "优先用户ID列表",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "优先用户在共享群组模式下不受群成员共享池限制（享有独立配额），但仍受日/周/月/时间段及用户特定限制约束。"
      },
      "group_mode_settings": {
        "description": "群组配额模式",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "格式：群ID:shared（共享池）或 群ID:individual（各自独立）。未设置默认共享。每个群占一行，如 123456:shared。"
      },
      "user_limits": {
        "description": "用户特定限制",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "格式：用户ID:次数，覆盖默认限额。例如 123456789:100 表示该用户每天可调用 100 次。优先级：用户特定 > 群组特定 > 默认。"
      },
      "group_limits": {
        "description": "群组特定限制",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "格式：群ID:次数，覆盖默认限额。例如 123456:50 表示该群每天可调用 50 次。优先级：用户特定 > 群组特定 > 默认。"
      },
      "time_period_limits": {
        "description": "时间段限制",
        "type": "list",
        "default": [],
        "items": {
          "type": "string"
        },
        "hint": "格式：HH:MM-HH:MM:次数[:enabled]。HH=0-23，MM=0-59，支持跨天时段（如 22:00-06:00）。次数为该时段内最大调用次数。:enabled 可省略，默认 true。"
      },
      "skip_patterns": {
        "description": "跳过消息前缀",
        "type": "list",
        "default": ["#", "*"],
        "items": {
          "type": "string"
        },
        "hint": "以此字符串开头的消息跳过限流检查且不计入使用次数。建议用指令前缀（如 #、/、*），避免系统消息误算到次数中。"
      }
    }
  },
  "messages": {
    "description": "消息设置",
    "type": "object",
    "items": {
      "show_remaining_count": {
        "description": "是否在达到限制时向用户显示剩余次数",
        "type": "bool",
        "default": true,
        "hint": "开启后，拦截消息会显示用户已用次数 / 总额度，帮助用户了解自身用量情况。关闭则只返回提示文本，不暴露具体数字。"
      },
      "usage_tip": {
        "description": "使用提示消息",
        "type": "text",
        "default": "您今日的 LLM 调用次数已用完（{usage}/{limit}）。请在 {reset_time} 后重试。",
        "hint": "可用占位符：\n{usage}=已用次数\n{limit}=总额度\n{remaining}=剩余次数\n{limit_type}=限制类型中文名（日限/周限/月限/当前时段）\n{reset_time}=下次重置时间（如 00:00 / 周一 / 每月1日）"
      }
    }
  }
}
